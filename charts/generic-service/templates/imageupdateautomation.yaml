{{- if or (not .Values.tool) (eq .Values.tool "") (eq .Values.tool "flux") }}
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageUpdateAutomation
metadata:
  name: {{ .Release.Name | quote }}
  namespace: flux-system
spec:
  git:
    checkout:
      ref:
        branch: main
    commit:
      author:
        email: tithelybot@users.noreply.github.com
        name: tithelybot
      # Repaired: the old range-over-Changed-Images commit template throws on newer Flux
      # controllers (ResultV2). A static Helm-rendered message cannot break across versions.
      messageTemplate: |-
        {{ .Release.Name }}: automated image update by Flux
    push:
      branch: main
  interval: 1m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
  update:
    {{- template "path" . }}
    strategy: Setters
{{- end }}
