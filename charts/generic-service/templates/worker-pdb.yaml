{{- range $key, $value := .Values.worker }}
{{- $replicas := $value.replicaCount | default 1 }}
{{- if and $value.enabled (ge (int $replicas) 1) }}apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ $value.name | default "worker" }}-pdb
  labels:
    app.kubernetes.io/name: {{ $value.name | default "worker" }}
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: {{ $.Release.Name | quote }}
spec:
  minAvailable: {{ get (default dict $value.pdb) "minAvailable" | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ $value.name | default "worker" }}
      app.kubernetes.io/component: worker
      app.kubernetes.io/part-of: {{ $.Release.Name | quote }}
{{- end }}
{{- end }}
